<!-- Page to run encounter -->
<h2>Time to roll for initiative!</h2>

<h3><%= encounter.title %></h3>
<hr />

<% monsters.forEach(function(monster) { %>
    <div class="well">
        <h4><%= monster.name %></h4>
        <!-- TODO: Add init box, health, delete button  -->
        <form>
            <label for="updateOrder" class="sr-only">Add initiative roll</label>
            <input id="updateOrder" type="text" placeholder="<%= monster.initiative %>">
            <input id="updateOrder" type="text" placeholder="<%= monster.health %>">
            <input
        </form>
        <hr />
    </div>
<% }) %>